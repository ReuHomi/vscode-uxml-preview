# vscode-uxml-preview

이 파일이 이 레포의 정본 규칙이다. `CLAUDE.md`는 이 파일을 가리키는 포인터이며
규칙 본문을 갖지 않는다. 규칙을 고칠 때는 이 파일만 고친다.

---

## 이 레포가 무엇인가

Unity Editor 없이 VSCode 안에서 `.uxml` / `.uss` 파일을 렌더링해 보여주는 확장.

렌더링은 **이 레포가 하지 않는다.** npm 패키지 `uxml-preview`(코어)가 한다.
이 레포는 파일을 읽고, 감시하고, webview를 띄우고, 코어가 뱉은 것을 그대로
표면에 옮긴다. 그게 전부다.

| | 코어 (`uxml-preview`) | 이 레포 (확장) |
|---|---|---|
| 파싱·캐스케이드·레이아웃·페인팅 | ✅ | ❌ |
| 경고·진단 생성 | ✅ | ❌ (표시만) |
| 파일 읽기·감시 | ❌ | ✅ |
| webview 수명주기·메시징 | ❌ | ✅ |
| VSCode 설정·명령 | ❌ | ✅ |

---

## 절대 규칙

1. **확장은 렌더 로직을 갖지 않는다.** 파싱·스타일 계산·레이아웃 코드를 이 레포에
   쓰는 순간 규칙 위반이다. 예외 없다.
2. **코어 코드를 복사하지 않는다.** npm 의존성으로만 참조한다. 코어를 고쳐야 하면
   코어 레포에 이슈/PR을 넣고 버전을 올린다. 여기서 우회하지 않는다.
3. **편집 기능을 만들지 않는다.** 이 확장은 뷰어다. 파일을 쓰지 않는다.
   드래그·리사이즈·속성 편집·요소 선택 전부 금지. 하나라도 들어오면
   UI Builder 복제로 가는 경사면이 시작되고, 그건 이 프로젝트가 명시적으로
   하지 않기로 한 일이다.
4. **남의 영역을 침범하지 않는다.** 구문 강조는 MS 공식 확장이, USS 검증·
   IntelliSense는 `helizac.uxml-uss-language-tools`와 Unity Code Pro가 이미 한다.
   `vscode.languages.createDiagnosticCollection`을 쓰지 마라. 진단은 webview
   안에만 그린다.
5. **못 그린 것을 숨기지 않는다.** 폴백으로 그린 요소, 해석 실패한 경로,
   미지원 속성은 반드시 사용자에게 보인다. 조용히 그럴듯하게 틀리는 것이
   이 프로젝트가 가장 싫어하는 실패 모드다.

---

## 렌더가 틀려 보일 때

이 항목이 이 파일에서 가장 중요하다.

**증상을 확장에서 고치지 마라.** 화면이 Unity와 다르면 원인은 셋 중 하나다.

| 원인 | 표시 | 대응 |
|---|---|---|
| 코어가 아직 지원 안 하는 컨트롤/속성 | 코어가 경고를 낸다 | 그 경고를 표면에 옮겼는지 확인. 코어는 정상 동작 중 |
| 코어 버그 | 경고가 없는데 다르다 | **코어 레포에 이슈.** 여기서 보정하지 마라 |
| 알려진 불일치 | `KNOWN_DIVERGENCES`에 있다 | 정상. 진단 패널에 그 항목으로 표시 |

CSS 지식으로 "이게 맞는 값 같은데"라고 판단해 확장에서 값을 보정하는 것은
**절대 금지**다. USS는 CSS가 아니고, 차이는 이미 코어에서 Unity 실측으로
확정돼 있다. 대표적으로 UI Toolkit의 `flex-direction` 기본값은 `column`이고,
`z-index`가 없으며, 기본 테마 규칙은 명시도와 무관하게 저자 규칙에 진다.
이런 값을 CSS 직관으로 되돌리는 수정은 반드시 반려된다.

확장에서 렌더 결과에 손대는 코드는 그 자체로 규칙 1 위반이다.
막히면 고치지 말고 보고하라.

---

## ponytail 예외 — 지우면 안 되는 것 3가지

이 레포는 ponytail 원칙에 맞다. 확장은 얇을수록 좋고, 대부분의 추상화는
불필요하다. 사다리를 그대로 적용하라.

**다만 아래 셋은 의도적으로 남긴 것이며 삭제 대상이 아니다.** YAGNI로 보이는
것이 정상이고, 그렇게 보이도록 만들어진 것이다.

### 예외 1 — 상태 맵 인자 (항상 빈 맵)

코어 호출부는 요소별 pseudo-class 상태 맵을 인자로 받는다. v1에서는 항상
빈 맵을 넘긴다. UI가 없기 때문이다.

지우면 안 되는 이유: 코어에는 상태 기능이 이미 완성돼 있고(S1에서 케이스로
판정 완료), 없는 건 UI뿐이다. v1.1에서 UI를 붙일 때 배선이 없으면
"webview로 무엇을 어떻게 보내나"를 처음부터 다시 설계하게 된다.

이 인자는 **선택적(`?`)이 아니라 필수**로 선언한다. 호출부가 명시적으로 빈 맵을
넘기게 만들어, 코드를 읽는 사람이 "왜 빈 맵이지?"를 반드시 묻게 하기 위함이다.
필수를 선택적으로 바꾸는 것도 삭제와 같은 위반이다.

재개 조건: S3에서 요소 선택 UI가 설계되거나, 실사용 피드백으로 요소 지정
방법이 정해지면.

### 예외 2 — 진단 패널

진단은 부가 기능이 아니다. **부분 커버리지가 성립하기 위한 조건**이다.
이 확장은 UXML의 일부만 그릴 수 있고, 무엇을 못 그렸는지 말하지 않으면
사용자가 잘못된 화면을 옳은 것으로 믿는다. "경고가 없으니 간단히 하자"는
방향의 축소 제안은 반려된다.

### 예외 3 — package.json 자기참조 CI 검사

`package.json`의 `name`이 `dependencies`의 어떤 이름과도 같지 않은지 검사하는
CI 단계가 있다. 추측성 방어가 아니라 재현된 사실에 대한 방어다.

Node는 패키지에 `exports` 필드가 있으면 자기 이름으로 자기를 import한다.
확장 이름이 `uxml-preview`였다면 `import 'uxml-preview'`가 코어가 아니라
**자기 자신으로 조용히 해석된다.** 에러도 경고도 나지 않는다. 실험으로
확인됐다. 지금은 `exports`가 없어서 안 걸리지만, 누가 추가하는 순간 터진다.

---

## 코드 규약 — LCC (Local Contract Coding)

목표는 **다음 사람이 오해 없이 확장할 수 있는 코드**다. 막으려는 실패는 둘이다 —
숨은 결합을 잘못 읽는 것, 검증되지 않은 가정 위에 쌓는 것.

이 레포에는 근거가 하나 더 있다. **디프만 읽는 리뷰어가 있다.**
리뷰 세션은 토큰을 아끼려 레포를 재독하지 않고 디프만 본다. 함수 헤더가 디프에
같이 들어오면 주변 파일을 열지 않고 판독이 되고, 없으면 열어야 한다.
헤더 네 줄이 파일 열람 여러 번을 대신한다.

### 제1원칙: 자명하지 않은 것만 적는다

코드가 이미 보여주는 걸 반복하지 않는다.
과잉 문서는 잡음이고, **읽는 쪽은 잡음을 신호만큼 성실하게 읽는다.**

붙일지 말지의 판별 질문은 이것 하나다 —
**"이 함수를 지우고 이름만 남겼을 때, 다음 사람이 잘못 만들 여지가 있는가?"**
없으면 붙이지 마라. 대부분의 함수에는 없다.

전부에 붙이는 것은 하나도 안 붙이는 것만큼 나쁘다. 전부에 있으면 어느 것이
중요한지가 사라진다.

### 드러낼 것 (가치 순)

1. **Deps/Effects** — 읽는 외부 상태, 발생시키는 이벤트, 요구되는 호출 순서,
   **소유권(누가 해제하는가)**. 본문만 봐서는 알 수 없다. 이 레포에서 가장 가치가 높다
2. **Purpose** — 코드가 보여줄 수 없는 "왜"
3. **Ensures** — 자명하지 않은 사후조건만
4. **Requires** — 호출자가 지켜야 하는 암묵적 사전조건만. strict가 대부분을
   타입으로 잡으므로 드물어야 한다

### 순수성 — 이 레포에서는 방향이 반대다

코어는 파싱·캐스케이드가 다수라 순수가 기본값이었다. **이 레포는 반대다.**
파일 I/O, VSCode API, webview 수명주기 — 거의 전부가 불순하다.
"순수하게 써라"는 여기서 지킬 수 없는 규칙이므로 적용하지 않는다.

대신 이렇게 뒤집어 쓴다.

**순수 함수가 필요해졌다는 것은 대체로 나쁜 신호다.**
확장은 배선이지 계산이 아니다. 단위 테스트가 필요할 만큼 로직이 쌓인 함수가
생겼다면, 먼저 물어라 — **이거 코어가 해야 하는 일 아닌가?**
절대 규칙 1 위반의 징후일 때가 많다.

정당한 순수 로직은 좁다. 지금까지 정당하다고 판단된 것:

- 임포트 프리페치 루프 (`src/preview/imports.ts`) — 코어의 `parse`를 도구로 쓸 뿐,
  스타일 의미를 계산하지 않는다
- 경로·GUID 해석 — 파일시스템 조회를 인자로 받아 분리
- CSP 문자열 조립

이 목록에 새 항목을 추가하려면 보고하고 승인받아라.

### 소유권 — 이 레포의 1급 위험

코어에서 GC되지 않는 Yoga 노드가 차지하던 자리를, 여기서는 `Disposable`이
차지한다. 실패 형태가 같다 — 자동으로 회수되지 않고, 코드만 봐서는 누가
해제하는지 안 보이며, 새어도 즉시 티가 나지 않는다.

`Disposable`을 만드는 곳에는 **누가 해제하는지 반드시 적는다.**
대상: webview 패널, `FileSystemWatcher`, `onDidReceiveMessage` 리스너,
`onDidChangeConfiguration` 구독, `registerCommand`.

패널이 닫힌 뒤에도 살아 있는 감시자는 파일이 바뀔 때마다 죽은 webview에
메시지를 보낸다. 예외는 나지 않는다. **조용히 틀리는 것**이고, 이 레포가
가장 경계하는 실패 형태다.

### 근거를 밝힌다 — 코어 계약과 VSCode 제약

임의로 보이지만 실은 강제된 선택에는 근거를 남긴다. 이게 없으면 다음 사람이
"더 간단히 할 수 있는데"라고 판단해 되돌린다.

```ts
// core: resolveImport is synchronous — the map must be complete before render.
// vscode: webview has no filesystem; this cannot become a lazy read.
```

USS 고유 동작을 근거로 무언가를 쓰고 있다면, 그 사실 자체를 먼저 의심하라.
이 레포에 USS 의미론이 들어올 자리는 거의 없다. 정말 필요하면 코어 문서의
절을 인용하되, 보고 항목에도 적어라.

### 헤더 형식 (함수마다 선택, 쓸 때는 고정)

TypeScript이므로 `///`가 아니라 JSDoc을 쓴다 — `///`는 TS에서 triple-slash
지시자라 의미가 충돌한다.

```ts
/**
 * Purpose:      자명하지 않은 의도
 * Deps/Effects: 읽는 상태 / 이벤트 / 호출 순서 / 소유권
 * Ensures:      자명하지 않은 사후조건
 * Requires:     암묵적 사전조건
 */
```

쓸 때는 이 이름을 이 순서로 쓴다. 읽는 쪽이 어디를 볼지 학습하게 하기 위해서다.
기존 파일들이 이미 이 형식을 쓰고 있으니 그것을 따르라.

### 조용히 어긋날 수 있는 계약은 묶어둔다

이 레포에서 조용히 갈라지는 계약은 둘이다.

1. **호스트 ↔ webview 메시지** — 양쪽이 다른 번들이라 런타임에 타입이 없다.
   `src/preview/protocol.ts`의 타입을 양쪽이 공유하는 것으로 강제하고,
   메시지를 손으로 만드는 코드를 두지 마라
2. **CSP 문자열** — 틀리면 증상이 "빈 화면"이라 원인이 안 보인다.
   `src/preview/csp.ts` 밖에서 CSP를 조립하지 마라

### 하지 않는다

- 코드가 이미 명백히 보여주는 것을 다시 쓰지 않는다
- 전체 설계를 함수 헤더에 넣지 않는다. 그건 architecture 문서 소관이다
- 주석으로 규칙을 지키게 하려 하지 않는다. **타입이나 테스트로 강제할 수 있으면
  그쪽을 쓴다.** 이 레포에 그 전례가 셋 있다 — 상태 맵의 필수 인자,
  자기참조 CI 검사, 규칙 파일 중복 검사

---

## 작업 계약

### 한 번에 스텝 하나

작업은 Step 단위로 주어진다. **주어진 Step의 범위를 스스로 넓히지 마라.**
다음 Step이 명백해 보여도 손대지 않는다. "이왕 하는 김에"가 이 레포에서
가장 흔한 실패다.

범위 밖에서 고쳐야 할 것을 발견하면 고치지 말고 보고 항목에 적어라.

### 기대값이 걸린 케이스는 구현보다 먼저 쓴다

구현을 보고 케이스를 쓰면 자기 구현을 정답으로 만든다.
기대값이 이미 주어진 경우 그 값을 바꾸지 마라. 통과하지 않으면 구현이 틀린
것이지 기대값이 틀린 것이 아니다. 기대값이 틀렸다고 판단되면 **바꾸지 말고
보고**하라.

### 테스트가 무엇을 잡는지 변이로 확인한다

테스트를 새로 썼으면, 구현을 일부러 되돌려 그 테스트가 실패하는지 확인하라.
되돌렸는데 통과하면 그 테스트는 아무것도 증명하지 않는다.

### 계측기를 먼저 의심하라

결과가 이상하면 코드보다 도구를 먼저 검사하라. 이 프로젝트는 셸 치환이 안 먹어
같은 패치를 네 번 돌린 적이 있고, 덤프의 중복 키가 눈을 가린 적이 있다.
"이상한 결과"의 원인이 계측기였던 비율이 낮지 않다.

### 정지 규칙

**같은 문제에 접근을 3번 바꿔 시도했는데 안 되면 멈추고 보고하라.**
4번째를 시도하지 마라. 포기가 아니라 비용 상한이다.

보고에는 세 가지를 적는다: 시도한 접근 3개, 각각이 어떻게 실패했는지,
막힌 지점이 어느 층인지(확장 / 코어 / VSCode API / 계측기).

### 커밋하지 않는다

커밋 메시지와 devlog는 이 세션이 작성하지 않는다. 작업이 끝나면 변경사항을
스테이지까지만 하고 보고하라. 커밋 메시지를 스스로 짓지 마라.

---

## 보고 형식

작업 종료 시 아래 형식으로 보고한다. 산문으로 길게 쓰지 마라.

```
## 한 일
- (변경한 파일과 그 이유. 파일당 한 줄)

## 테스트
- 추가: (개수) / 통과: (개수) / 실패: (개수)
- 변이 확인: (되돌려서 실패하는 것을 확인했는가 — 예/아니오/해당없음)

## 범위 밖에서 발견한 것
- (고치지 않고 적기만 한 것들. 없으면 "없음")

## 판단이 필요한 것
- (내가 정하면 안 되는 것들. 없으면 "없음")

## 막힌 것
- (정지 규칙에 걸린 것. 없으면 "없음")
```

---

## 숫자를 다룰 때

정확도 수치를 인용할 때는 **한정어를 같이 옮긴다.** 이 프로젝트에는 뜻이
다른 숫자가 여럿 돌아다닌다.

- **값 일치** — 비교 좌표 중 일치한 수. 대외 숫자
- **케이스 일치** — 완전히 일치한 케이스 수. 내부용
- **기준값 확보** — Unity 실측값을 확보한 케이스 수. **정확도가 아니다.**
  이것을 정확도로 인용한 사고가 실제로 있었다

현행 값은 코어 레포의 `docs/accuracy.md`가 단일 진실이다. 다른 곳에서 본
숫자를 옮기지 말고 거기서 확인하라. 커버리지와 정확도는 다른 것이다.

---

## 스택

TypeScript / esbuild / VSCode Extension API / Vitest

확장 ID `reuhomi.vscode-uxml-preview`, 설정 네임스페이스 `uxmlPreview.*`,
명령 `uxmlPreview.showPreview` · `uxmlPreview.showPreviewToSide`,
webview viewType `uxmlPreview.panel`.

**이 식별자들은 전부 일방향 문이다.** 설정 키를 바꾸면 사용자의
`settings.json`이 조용히 무시되고, viewType을 바꾸면 열어뒀던 패널이 복원되지
않는다. 개명 제안을 하지 마라.
