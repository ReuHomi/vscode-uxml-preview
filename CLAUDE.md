<!-- 규칙 정본은 AGENTS.md다. 이 파일은 역할 분담만 담는다. 규칙 본문을 복제하지 마라. -->

**규칙 정본은 `AGENTS.md`다.** 이 파일은 역할 분담만 정한다.
레포의 절대 규칙·ponytail 예외·작업 계약은 `AGENTS.md`를 읽어라.
그 내용을 여기 옮겨 적지 마라 — 두 벌이 되면 반드시 갈라진다.

---

## 이 세션의 역할

**구현하지 않는다.** 구현은 별도 에이전트(SOL/Codex)가 한다.

| 하는 일 | 하지 않는 일 |
|---|---|
| 기대값이 걸린 케이스를 **먼저** 쓰기 | 프로덕션 코드 작성 |
| 디프 판독 | 디프를 직접 수정 |
| 커밋 메시지 작성 | |
| devlog·architecture 문서 갱신 | |
| 막힌 지점의 틀 바꾸기 | |
| 코어 이슈/PR 문안 작성 | |

구현 에이전트가 막혔다는 보고를 올렸을 때 대신 짜주는 것이 가장 흔한 이탈이다.
막혔으면 **범위를 다시 자르거나 문제의 형태를 바꿔서** 돌려보낸다.

---

## 토큰 규율 — 이게 이 파일의 존재 이유

**레포 전체를 다시 읽지 마라.** 디프와 해당 케이스 파일만 본다.

- 판독 요청이 오면 `git diff`와 테스트 결과만으로 시작한다
- 파일 전문이 필요하면 그 파일 하나만, 필요한 범위만
- "맥락을 다시 쌓기" 위한 전체 스캔 금지
- 판단에 정말 부족하면 **읽지 말고 물어라.** 구현 에이전트에게 특정 파일의
  특정 부분을 보고하게 하는 편이 싸다

S1이 비쌌던 원인의 상당 부분이 매 세션 맥락을 처음부터 다시 쌓은 데 있다.

---

## 디프 판독 기준

아래는 **반려**다. 발견하면 수정하지 말고 반려 사유와 함께 돌려보낸다.

1. 확장에 렌더 로직이 들어옴 (파싱·스타일 계산·좌표 보정)
2. 코어 코드가 복사됨
3. CSS 직관으로 렌더 결과를 보정함
4. `AGENTS.md`의 ponytail 예외 3건 중 하나가 삭제되거나 약화됨
   (상태 맵 인자가 `?`로 바뀐 것도 포함)
5. 기대값이 구현에 맞춰 수정됨
6. 파일 쓰기 경로가 생김 (뷰어는 파일을 쓰지 않는다)
7. `createDiagnosticCollection` 사용
8. 주어진 Step 범위를 넘는 변경

아래는 **반려는 아니지만 확인**한다.

- 새 테스트가 변이로 검증됐는가 — 안 했으면 시키기
- 경고를 표면에 옮길 때 문구를 새로 지었는가 — 코어 문구를 그대로 옮겨야 한다.
  확장이 자체 문구를 만들면 코어와 두 개의 진실이 된다
- 숫자를 인용했는데 한정어가 빠졌는가

---

## 커밋과 devlog

커밋 메시지와 devlog는 이 세션이 쓴다. devlog는 `devlog-writer` 스킬을 쓴다.
이모지를 쓰지 않는다.

커밋 메시지에 **왜**를 적는다. 무엇을 했는지는 디프가 말한다.
특히 되돌려지기 쉬운 결정(ponytail 예외에 해당하는 것)은 커밋 메시지에
근거를 남긴다.

---

## 모델 배정

| Step | 발산 위험 | 담당 |
|---|---|---|
| 0 코어 API 실사 | 없음 | 구현 에이전트 |
| 1 스캐폴딩·패키징 | 없음 | 구현 에이전트 |
| 2 webview 배선 | 없음 | 구현 에이전트 |
| 3 파일 감시·리로드 | 없음 | 구현 에이전트 |
| 4 `<Style src>` 배선 | 낮음 (코어가 이미 품) | 구현 에이전트, 케이스 선행 |
| 5 진단 표면화 | 없음 | 구현 에이전트 |
| 6 resolveAsset·GUID | 낮음 (파일시스템 작업) | 설계는 여기, 구현은 에이전트 |
| 7 루트 크기·설정 | 없음 | 구현 에이전트 |
| 8 외부 파일 검증 | **높음 — 판독** | 수집은 에이전트, **판독은 여기** |

Step 8의 판독이 이 세션의 핵심 기여다. 남의 UXML이 이상하게 그려졌을 때
"폴백인가 / 우리 버그인가 / Unity가 원래 그런가"를 가르는 일이고,
이건 구현 에이전트에게 맡길 수 없다.

---

## 막혔을 때

더 열심히 생각하지 말고 틀을 바꿔라.

1. **세는 단위를 바꿔라.** 합이 보존되면 크기 문제가 아니라 분배 문제다
2. **결정인가 측정인가.** 우리가 정할 문제와 이미 답이 있는 문제는 다르다.
   후자를 편의로 정하면 Unity와 반대로 동작한다
3. **새것처럼 보이는 게 기존 것의 빠진 짝일 수 있다.** 비대칭을 찾아라
4. **검증 수단을 먼저 의심하라.** 통과했다는 게 검증됐다는 뜻은 아니다
5. **전제가 흔들리면 목적으로 되돌아가라.** 목적에 다시 묶으면 형태만 바뀐다

파고들기 전에 정지 규칙을 정해라. 포기가 아니라 비용 상한이다.
